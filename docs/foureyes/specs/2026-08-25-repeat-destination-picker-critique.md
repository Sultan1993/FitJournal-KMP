# Critique log — Repeat destination picker

Dispositions: `fixed` (drafter changed the artifact), `rejected` (coordinator
fact-checked it and it was false — with what was checked), `intentional`
(drafter kept the design and said why), `open` (seam ended on `final`/`conclude`
with this unaddressed).

## spec · round 1 · 2026-08-25 · 339s
- [Critical] fixed — Refusal can raise the paywall before the sheet is actually closed; nulling state + trySend only orders model mutations, and the proposed test observes nullable state rather than the sheet's disappearance. Drafter replaced the "close then emit" with a pending-outcome + `RepeatPickerClosed` composition-exit handshake dispatched from `onDispose`, and retargeted the tests at rendered ordering.
- [Critical] fixed — Assumption 10 synthesized an empty day from a failed read. Coordinator CONFIRMED in `RepeatDestination.kt`: with empty inputs `repeatDestinations` returns `Single(destination(1))` where `isNewWorkout = 1 !in emptySet()` = true, so a transient read failure silently offers a NEW page on a day that really holds pages — violating locked decisions 2 and 3 and the user's "no implicit logic" principle. Drafter added an explicit `Content.LoadFailed` state: Add disabled, visible Retry, never degraded to `Single`.
- [Important] fixed — The zero-native-commits proof could not detect COMMITTED native changes (clean `git status` proves only a clean worktree). Drafter added criterion 0: record both native repos' baseline SHAs and assert them unchanged at the end, alongside clean-status and green builds.
- [Important] fixed — Test plan did not measurably establish locked decisions 1, 10 and 12 (initial date is today, Change swaps panes within one sheet, no page-number eyebrows, every source exercise copied). Drafter added focused VM / screen / repository assertions for each.
- [Minor] fixed — §7's deletion checklist and proof grep omitted `WorkoutDetailsViewModel.quotaGate`, which §3.5 removes; an unused constructor field would still compile. Drafter added it plus a scoped second proof grep (the name has legitimate surviving homes elsewhere).
- [Minor] fixed — §9.7 required "exactly three commits in Multiplatform", which is outside the brief and fails a correct squashed implementation. Drafter deleted the commit-count criterion; the real constraint (native trees identical to baseline) is criteria 2–3.

## spec · round 2 · 2026-08-25 · 268s
- [Critical] fixed — `onDispose` is not a modal-dismissal completion: a sibling `DisposableEffect(Unit)` has no documented ordering against `ModalBottomSheet`'s internal dialog disposer, so the round-1 handshake did not actually prove locked decision 7. Drafter adopted Material3's documented choreography instead: the screen hoists `SheetState`, the VM flips a `closing` flag, the screen awaits `sheetState.hide()` and only then dispatches `RepeatPickerClosed`. Added a guard so a racing `RepeatPickerDismissed` cannot drop a pending paywall.
- [Important] fixed — Assumption 7 (rank by exercise count when nothing is logged) contradicted §3.5's promise to reuse `rankedMuscles` unchanged. Coordinator CONFIRMED in `WorkoutDetailsUiBuilder.kt`: `rankedMuscles` only counts exercises where `logged > 0`, so a blank-template page returns an EMPTY ranking. Drafter kept the shared helper byte-identical (visibility change only) and layered a picker-private `rankByExerciseCount` fallback on top, with a blank-template row test.
- [Important] fixed — Calendar-month reads had no failure path while §5 claimed every VM IO entry point was wrapped. Coordinator CONFIRMED `ImportWorkoutViewModel.loadWorkoutDays` is a bare `viewModelScope.launch` with no catch — copying it would put an uncaught throw in a VM scope (the iOS SIGABRT class). Drafter added assumption 13: cancellation rethrown, other throws logged, `workoutDays` preserved, plus a failure test. Import's own gap recorded as pre-existing and out of scope.
- [Minor] fixed — Assumption 2 justified dropping the mock's calendar-confirm button by appeal to locked decision 5, which only governs the destination CTA's static "Add" text. Drafter re-justified auto-commit on its own merits (consistency with Import's calendar, one fewer tap, one unambiguous commit CTA) and noted the mock override explicitly in the header.

## plan · round 1 · 2026-08-25 · 493s
- [Critical] fixed — The disabled Add button could not be implemented inside Task 7's file list: `FjPrimaryButton` has no `enabled` parameter and installs `.clickable(onClick = onClick)` unconditionally, so a faithful implementation would have made an undeclared edit to the shared `ui/common/FjPrimaryButton.kt` — a write race under the parallel-wave model. Coordinator CONFIRMED by reading the file. Drafter promoted it to its own wave-1 task (new Task 3) with an additive `enabled: Boolean = true` placed before `leadingIcon`, `clickable(enabled = enabled, ...)`, and alpha-modulated disabled visuals on the existing brand token.
- [Critical] fixed — Picker disposal had no callable contract: `RepeatPickerContract.ViewModel` exposes only `viewState`/`dispatch`, yet the details VM was required to call `dispose()` on three teardown paths, with no owner handle or cast pinned. Drafter pinned a concrete `private var repeatPickerVm: RepeatPickerViewModel?` owner field alongside the interface-typed state field, set and nulled together, no cast anywhere.
- [Critical] fixed — Task 5's quota-gate test guidance named APIs that do not exist. Coordinator CONFIRMED: `class WorkoutQuotaGate(private val records: RecordRepository)` is final with exactly one dependency, so neither "subclass it" nor "fake quota source" is possible. Drafter points the implementer at the two working in-repo precedents (`WorkoutQuotaGateTest.kt:82`, `WorkoutListViewModelQuotaTest.kt:188`). **CORRECTION (added at plan round 2):** the coordinator initially recorded Sol's `FreeQuotaSettings` remedy as "partly wrong" and instructed the drafter to ignore it. That was the coordinator's error, not Sol's. `WorkoutQuotaGateTest` uses a real `DefaultRecordRepository` over in-memory SQLite (NOT a hand-rolled fake, as the coordinator told the drafter), and `FreeQuotaSettings` is a global `object` the test resets in `@BeforeTest`/`@AfterTest` and configures via a `meterOn(limit)` helper — exactly what Sol prescribed. Round 2 re-raised this as a Critical and it was fixed there.
- [Important] fixed — A literal `muscleTitleFormatter = <existing field>` placeholder survived in Task 10's "exact" constructor call. Coordinator confirmed the VM's real field is named `muscleTitleFormatter`; drafter substituted it and clarified that userId/journalId come from the VM's `userContext`-derived identity rather than bare fields.
- [Important] fixed — `maxDate` had no independent verification: Task 2 only grepped for the identifier and its claimed "preview-checked" coverage was never created. Drafter added a runnable `WorkoutCalendarTest` proving a future day reports disabled semantics and never invokes `onDateSelected`, plus an allowed-day control.
- [Important] fixed — Several `blockedBy` edges serialized fully pinned, file-disjoint work with no data dependency. Drafter added an explicit dependency-edge policy (edges only where a task must read a predecessor's FINISHED file), dropped the unnecessary ones, and left a one-line rationale on each retained edge. Wave 1 widened from 6 tasks to 8.
- [Important] fixed — The native gates compared SHA prefixes where the spec requires equality with the recorded baselines. Coordinator re-read both repos and confirmed Sol's full SHAs exactly. Gates now use `test "$(git rev-parse HEAD)" = "<40-char SHA>"`.
- [Minor] fixed — Two stale source references. Coordinator CONFIRMED both: `ConfirmActionSheet.kt` is under `ui/common/`, not `ui/workout/details/components/`; and `WorkoutCalendar` has THREE call sites (`ImportWorkoutScreen`, `WorkoutListScreen`, `WorkoutScreen`), not the two the plan named — an incomplete inventory behind a "leave call sites untouched" criterion.

## plan · round 2 · 2026-08-25 · 410s
- [Critical] fixed — An interrupted sheet hide could lose the pending outcome forever. Coordinator CONFIRMED against the real Material3 source (1.5.0-alpha17 sources jar, `SheetDefaults.kt`): `SheetState.hide()` carries `@throws [CancellationException] if the animation is interrupted`. A user grabbing the sheet mid-close would kill the effect coroutine before `RepeatPickerClosed` was dispatched, while the VM ignores `RepeatPickerDismissed` during `closing` — stranding the paywall/navigation with no recovery. NOTE the provenance: this handshake existed only because Sol's own spec-round-2 Critical (correctly) rejected an `onDispose`-based close; the replacement introduced this. Drafter added an `internal suspend fun awaitSheetHidden(isVisible, hide)` that retries an interrupted hide while `currentCoroutineContext().isActive` and rethrows a genuine composition cancellation, made `RepeatPickerClosed` idempotent in the VM, and added three race unit tests.
- [Critical] fixed — The pinned use-case code failed its own required proof: `println("[FJ_QUOTA] canWriteWorkout threw…")` meant `rg -c 'canWriteWorkout'` returned 2, so spec §9.5 and the Task 13 gate would both fail on a CORRECT implementation. Coordinator CONFIRMED by running the count over the pinned lines. Drafter reworded the log to "quota check threw" and made Tasks 6 and 13 agree.
- [Critical] fixed — The quota-gate test setup could not reach refusal or throw at all: on `FreeQuotaSettings` reset defaults the gate resolves `Unlimited` before touching the repository, so every refusal test would have passed for the wrong reason. Coordinator CONFIRMED and, in doing so, RETRACTED its own round-1 note (see the correction above): `WorkoutQuotaGateTest` uses a real `DefaultRecordRepository` over in-memory SQLite, and `FreeQuotaSettings` is a global object it resets in `@BeforeTest`/`@AfterTest` with a `meterOn(limit)` helper — exactly Sol's round-1 prescription. Drafter rewrote Task 6/10/11 test guidance to mirror that discipline and pinned which scenarios need `meterOn()`.
- [Important] fixed — Task 13's gate had prose-only proofs for §9.6 and half of §9.7 while its `verifyCommand` ran only `:shared:jvmTest`. Drafter supplied a complete zero-safe zsh block, including `!`-inverted "must print nothing" checks (rg exits non-zero on zero matches) and a guarded `DisposableEffect` comparison against base `3bd3c4c`.

## code · round 1 · 2026-08-25 · 335s
- (none)

Sol reviewed the full 18-commit range (a32cb2d..53eeb53) after all three gates were
green, and returned `VERDICT: pass` with no findings. It was asked specifically to
attack the close handshake's interleavings, to hunt for tests that assert falsely
(a real one had just been found at the gate), the thrown-read-never-becomes-Single
rule, single-gate-call-against-the-resolved-slot, and any path by which this KMP
change could still force a native edit.
