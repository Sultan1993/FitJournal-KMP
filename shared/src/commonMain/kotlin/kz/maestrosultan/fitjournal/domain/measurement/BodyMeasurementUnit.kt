package kz.maestrosultan.fitjournal.domain.measurement

/**
 * Display unit family. Drives which formatter the cell uses and which
 * label suffix (kg/lbs, cm/in, %, photo count) is rendered.
 */
enum class BodyMeasurementUnit { WEIGHT, PERCENTAGE, PHOTO, SIZE }
